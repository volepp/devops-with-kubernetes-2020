server {

    listen 80;

    root /usr/share/nginx/html/;

    index index.html;

    location /healthz {
        return 200 "health ok";
    }

    location / {
        try_files $uri /index.html;
    }

    location ~* \.(?:css|js)$ {
        try_files $uri =404;
    }
}