http {
    server {

        listen 80;

        root /usr/share/nginx/html/;

        index index.html;

        location /healthz {
            return 200 "health ok";
        }

        location / {
            try_files $uri /index.html;
        }
    }
}