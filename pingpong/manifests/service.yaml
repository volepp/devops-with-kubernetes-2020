apiVersion: v1
kind: Service
metadata:
    namespace: main-application
    name: dwk-pingpong-svc
spec:
    type: ClusterIP 
    selector:
        app: dwk-pingpong
    ports:
        - port: 2345
          protocol: TCP
          targetPort: 3000
